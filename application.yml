logging:
  file:
    name: e://logs//config-client.log

management:
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true  
      
  endpoints:
    web:
      exposure:
        include:
        - "*"
